Original prompt: Create a browser-playable semi-realistic Sims-like house building game using three.js or another high-quality 3D engine. Users should pull assets from a left panel, drop them into the canvas, build/decorate a home, edit styles/colors, use quick room modules, design landscape, keep scale sensible, and save locally for later editing. User clarified: free orbit camera, quick full-room modules for now, outside/inside toggle, localStorage save, prototype scope, include suggested enhancements.

Progress:
- Generated a semi-realistic catalog thumbnail sheet using the imagegen skill and copied it to `public/assets/catalog-sheet.png`.
- Scaffolded a Vite/Three.js web app with left asset panel, toolbar, canvas, and inspector UI.
- Implemented first pass of Three.js builder systems in `src/main.js`: orbit camera, quick room modules, furniture/landscape asset factories, drag/drop placement, selection inspector, style/color editing, transform controls, save/load, undo/redo, inside/outside/walk views, and test hooks.
- Fixed `npm run build` script to use `vite build`; the previous script started a long-running Vite server.
- First Playwright run rendered the scene correctly, but found a console error from adding `TransformControls` directly to the scene. Patched to add `transform.getHelper()` for the current Three.js API.
- Targeted interaction run verified save/clear/load state behavior, but showed a washed-out inside screenshot and blank thumbnail crops on some cards. Adjusted inside camera preset and fixed catalog sprite background-position math.
- Final build passed. Required web-game Playwright client rendered screenshots successfully with no error artifact. Targeted Playwright interaction test placed a chair, edited color/style, toggled inside view, saved, cleared, loaded, and restored 9 objects with no page errors. Latest verified screenshot: `output/web-game/interaction-verified.png`.
- User requested scaling the app down into an individual room builder/decorator. Generated a new imagegen furniture catalog sheet at `public/assets/room-catalog-sheet.png`.
- Replaced the whole-house UI with room-studio controls: furniture categories, always-visible room width/depth/wall/floor controls, isometric/top view buttons, and room-focused inspector controls.
- Replaced the Three.js scene with one adjustable room shell and a furniture catalog covering sofa, armchair, chair, desk, computer, bookshelf, bed, wardrobe, dresser, plant, curtains, rug, TV, floor lamp, coffee table, and side table. Added direct click-drag movement on the floor plane plus rotate/scale/material/color editing.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright interaction test. The targeted test resized the room to 10m x 7.5m, placed a bookshelf, rotated/recolored it, dragged it across the room, saved, cleared, and loaded the design with no page errors. Verified screenshot: `output/web-game/room-studio-verified.png`.
- UX refinement: catalog selection is now a one-shot placement tool. Initial active item is null, clicking empty room without an armed item does not place furniture, placing an item clears both catalog and scene selection, clicking an item selects it for edit/drag/rotate, and clicking empty space deselects.
- Expanded catalog from 16 to 46 items, adding loveseat, stool, bench, ottoman, bean bag, laptop, drawing tablet, easel, cube shelf, wall shelf, book stack, basket, tall/small plants, round rug, mirror, wall art, clock, vase, pillows, blanket, candles, sculpture, speakers, game console, record player, table lamp, ceiling light, nightstand, and vanity.
- Verification passed again: `npm run build`, required web-game Playwright client, and targeted Playwright UX test. Targeted test confirmed one-shot placement, deselection, select-to-rotate, empty-click deselection, larger category counts, search results, and no page errors. Verified screenshot: `output/web-game/ux-items-verified.png`.
- Replaced repeated sprite-sheet thumbnails with geometry-rendered thumbnails generated from the same Three.js factories used for in-room objects. Removed the static catalog sheet preview from the sidebar. Armed placement now takes precedence over selecting existing furniture if the user has chosen an item and clicks inside the room.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted thumbnail/placement test. Confirmed stool, bench, and chair thumbnails are distinct data URLs, stool and bench place as different 3D objects, and placement clears active/selected state. Verified screenshot: `output/web-game/geometry-thumbnails-verified.png`. Browser emitted expected ReadPixels performance warnings during one-time thumbnail generation, but no page errors.
- Added Walk mode with first-person camera controls: click Walk, click the canvas to pointer-lock mouse look, use arrows/WASD to move, Esc releases pointer lock, Iso/Top returns to editor camera modes.
- Added HD toggle for higher-fidelity procedural material mode on walls, floors, furniture, and decor. It uses generated canvas textures/bumps over existing Three.js geometry; no imagegen call was needed for this first HD pass. For future true high-fidelity assets/textures, imagegen can generate texture atlases/reference art, while high-poly models would be best handled through glTF assets.
- Fixed UX/placement issues: category clicks clear the search field; laptop and drawing tablet were raised to sit on desks better; rectangular and round rugs were raised above the floor surface so they remain visible.
- Replaced runtime WebGL thumbnails with deterministic SVG silhouettes after WebGL thumbnails interfered with the main room canvas in Chromium. The SVGs remain distinct per asset and avoid canvas/context problems.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted test. Confirmed search clears on category click, HD toggles on, work thumbnails render, rug/laptop place, Walk mode enters and keyboard movement changes camera position, then Iso mode returns. Verified screenshot: `output/web-game/walk-hd-verified.png`.
- Latest room-builder refinement: default scene now starts empty, catalog selection remains a one-shot click-to-place flow, object moves still push undo/redo history without showing a move log hint, and rotation hotkeys were added (`R`/`E`/`]` clockwise, `Q`/`[` counterclockwise).
- Added All/Rooms/Build categories, three room module assets, door/window/archway structure assets, distinct SVG thumbnails for room and media assets, and room-aware placement. Furniture/structure is assigned to the room it is placed in; room modules can sit on the wider lot and snap beside the main room or other room modules.
- Added room container behavior: moving a room module moves its contained furniture, resizing a selected room clamps contained items, and deleting a room removes its contents. Door/window/archway entries snap to the nearest wall. Walk mode now starts at the front of the main room looking inward, supports click-drag mouse look, and can move across adjacent room module bounds.
- Verification passed again: `npm run build`, required web-game Playwright client, and targeted Playwright checks. Confirmed empty default state, 52 All assets, category-search clearing, 4 distinct media thumbnails, room/structure catalogs, two room placements, door/window/chair placement, hotkey rotation and reverse rotation, and walk-mode drag look/movement with no page errors. Verified screenshots: `output/web-game/multiroom-walk-verified.png` and `output/web-game/hotkey-selection-verified.png`.
- User reported that added rooms lacked floor grids, doors/windows did not cut walls, room wall shadows leaked into snapped rooms, and save/load needed named projects. Patched `src/main.js`, `index.html`, and `src/styles.css`.
- New rooms now get the same grid helper as the base room. Room walls are built from segments, and door/window/archway/front-door assets create visual wall openings. Openings are also mirrored into an adjacent snapped room when the structure sits on a shared wall line. Room-shell wall meshes no longer cast shadows, reducing the large wall-shadow bleed seen between rooms.
- Added kitchen, bath, outdoor, stairs, front-door, and roof assets with representative thumbnails and simple 3D geometry: counters, island, fridge, stove, sink, cabinet, toilet, tub, shower, bath vanity, stairs, grass plot, deck, patio, walkway, fence, tree, shrub, and BBQ.
- Added per-room locking via the inspector. Locked room modules can still be selected and edited but cannot be dragged accidentally; lock state is serialized.
- Replaced single-slot save/load UI with named local projects. Save opens a modal for naming or overwriting an existing project; Load opens a modal list of saved projects. Project name, lock state, outdoor/null room ownership, and object state survive reload.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright. Confirmed 73 assets in All, new category counts, outdoor/kitchen/bath placement, room-grid rendering in added room, wall door placement, room lock, named save, clear, modal load, and restored project/object state with no page errors. Verified screenshot: `output/web-game/rooms-openings-save-verified.png`.
- Next architecture pass: added a `Shell` toolbar mode for exterior viewing. When enabled, front walls are generated for every room shell while keeping door/window openings, making the house read as a closed exterior instead of an open cutaway. The button toggles back to the interior build view with open front walls.
- Front Door now snaps to the front wall even when the interior shell is open; normal door/window/archway behavior still uses nearest available walls. Wall-mounted objects (`curtains`, `mirror`, `wall-art`, `clock`, `wall-shelf`, `upper-cabinet`, and `television`) now snap to walls and rotate to match the wall instead of lying around as floor objects.
- Roof placement now snaps to the nearest room center and sizes itself to that room footprint with an overhang. The roof geometry was changed to a simple pitched roof with a ridge and two roof planes.
- Added a conservative footprint collision pass for floor furniture inside the same room. It prevents placing or dragging duplicate furniture into the same occupied footprint while leaving rooms, outdoor items, doors/openings, wall-mounted items, and roofs flexible.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright. Confirmed Shell mode state, front-door front-wall snapping, roof placement, wall-art wall snapping, and collision prevention with no page errors. Verified screenshots: `output/web-game/shell-roof-wall-verified.png` and `output/web-game/shell-front-roof-verified.png`.
- Added a Pets category with Cat, Dog, Pet Bed, Pet Bowl, Cat Tree, and Aquarium assets. These have distinct SVG thumbnails, simple 3D geometry, placement, collision footprints, and render-state counts.
- Improved room module snapping so rooms dropped partly over an existing room resolve to the nearest clean side attachment instead of remaining overlapped. This makes room-by-room building easier and gives roof clustering stable inputs.
- Cluster roof pass: roof placement now finds the connected room cluster nearest the click, stores the covered room ids, and sizes/centers the roof around the whole cluster bounding box rather than only one room.
- Walk-mode doorway rules now block movement through a shared wall unless the two adjacent rooms share a door or archway opening. Door/archway placement prefers a nearby shared wall between rooms, making room connections easier to place. Once connected, walk mode can pass between those rooms.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright. Confirmed pet catalog/placement, clean room snap, cluster roof dimensions/roomCluster state, wall blocking without a passage, and successful walk-through after adding an archway with no page errors. Verified screenshots: `output/web-game/pets-cluster-roof-verified.png` and `output/web-game/doorway-walk-pets-verified.png`.

TODO:
- Implemented the strongest next suggestion: style variants for placed elements. The inspector now has a Variant selector, variant state is saved/loaded, duplicated, and exposed in `render_game_to_text`. Current variants cover sofa, loveseat, chair, bed, desk, rug, round rug, roof, cat, dog, pet bed, and aquarium.
- Added a Duplicate inspector action plus `Ctrl/Cmd+D`, keeping the copy grid-offset and selected for quick room furnishing.
- Added basic pet idle animation for cats and dogs using lightweight per-part motion, including standing/curled/sleeping behavior.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright. Confirmed a bed can be changed to the canopy variant, duplicated with the variant preserved, and a dog can be placed afterward with no page errors. Verified screenshot: `output/web-game/variants-pets-qol-verified.png`.
- Builder ergonomics pass: converted room-only locking into global item locking. The inspector now says Lock item, `L` toggles lock/unlock for any selected object, locked items cannot be dragged/rotated/scaled/deleted, and the selection outline turns amber/unlocked or red/locked.
- Added `Ctrl/Cmd+C` and `Ctrl/Cmd+V` copy/paste for selected objects. Pasted copies preserve orientation/style but are unlocked and selected for editing.
- Enlarged room and property scale: main/selected room width and depth sliders now go up to 24m, free/outdoor placement supports a wider lot, orbit zoom can pull much farther back, and Top/Iso/Tour camera positions scale around the current property bounds.
- Added exterior wall finish support. Main room has an Exterior selector, room modules can use the inspector Material selector for exterior finish, and wall shells now keep painted interior walls while adding thin exterior skins with vinyl siding, brick, stone, or shingle procedural textures.
- Added Sliding Patio Door and French Patio Doors to Build, plus Wide Staircase and Stair Landing options to support second-floor planning before a full multi-floor engine exists.
- Added repeated-item orientation memory: after rotating an item such as a fence, the next placed item of the same kind uses the same rotation.
- Added Tour mode for a slow overhead rotating "helicopter ride" camera around the current property.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright. Confirmed 20m x 18m room sizing, brick exterior state, fence selection outline, `L` lock, locked drag protection, `Ctrl+C`/`Ctrl+V`, pasted orientation preservation, remembered fence orientation, sliding patio door placement, and active moving Tour camera with no page errors. Verified screenshot: `output/web-game/lock-copy-exterior-tour-verified.png`.
- Room refinement pass: the initial room is now selectable like a room module, starts locked by default, can be unlocked with the normal lock action/hotkey, and can be edited through the room size/color/exterior controls. It remains the fixed anchor room and cannot be copied/deleted.
- Shared/interior room walls no longer receive exterior skins. Wall cores stay painted, and siding/brick/stone/shingle procedural skins are only added to wall sides with no adjacent room.
- Room module placement now inherits the last room exterior material used, so added rooms follow the previous room's siding/brick/stone/shingle choice.
- Added a starter grass lot under the default larger 12m x 10m room, plus outdoor activity assets: Outdoor Bar, Fire Pit, Horseshoes, and Volleyball Net.
- Added a `Roofs` toolbar toggle that hides/shows roof objects independently from Shell mode, so users can temporarily remove roofs from view while decorating interiors.
- Added Ctrl/Cmd multi-select for scene objects. Ctrl/Cmd-click toggles items into the selection, and dragging one selected item moves the selected group together.
- Reduced room-edge clamp margins for smaller objects like plants and pets so they can be moved closer to walls.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright checks. Confirmed default room size/lock, initial-room selection/unlock, room material inheritance, roof visibility state, outdoor activity catalog entries, two-room placement, and two-fence Ctrl multi-select movement. Verified screenshots: `output/web-game/room-selection-roof-grass-verified.png` and `output/web-game/multiselect-room-refine-verified.png`.
- Picking refinement: hidden roofs are now excluded from ray picking, and hiding roofs clears roof selections. Locked objects no longer block editable objects behind them; picking prefers the nearest unlocked hit and falls back to locked hits only when there is no editable target behind. Verification passed with `npm run build`, required web-game Playwright client, and targeted Playwright roof/locked picking test. Screenshot: `output/web-game/roof-hidden-locked-picking-verified.png`.
- Latest refinement pass: door openings now segment the baseboard/moulding instead of removing trim from the entire wall; trim remains on both sides of a door opening.
- Exterior wall material skins are now applied by exposed wall segment instead of by whole wall side, so shared/interior overlap stays painted while uncovered exterior portions keep brick/siding/stone/shingle finishes.
- Roof sizing now uses a shared overhang constant, starts at the correct initial footprint, and updates automatically when room dimensions or room shells change.
- Placed items are now selected immediately after drop, making rotate/scale/color/variant changes available without another click.
- Fixed and expanded Variant behavior with additional variants for TV, decks, stairs, and wide stairs. Added a raised deck variant and stair-with-landing variants to make second-level planning clearer.
- Most objects can now be placed on the lot outside rooms, including TV/outdoor entertainment setups, while wall openings still require a room wall.
- Verification passed: `npm run build`, required web-game Playwright client, and targeted Playwright checks. Confirmed immediate selection after placement, variant selector options and application, roof initial sizing/resizing, outside TV placement, raised deck variant, room exterior material inheritance, and attached room placement. Verified screenshot: `output/web-game/roof-variants-door-outdoor-verified.png`.
- Project setup pass: renamed the local folder from `vs` to `house-builder-app`, renamed the package to `house-builder-app`, added a Vite/Node `.gitignore`, added a README, initialized Git on `main`, and created the first local commit. Ignored artifacts include `node_modules/`, `dist/`, `output/`, logs, cache folders, coverage/test reports, editor folders, OS files, and `.env` secrets.
- Dev server is running from `C:\Users\TheRi\OneDrive\Desktop\projects\web\house-builder-app` at `http://127.0.0.1:5173/`. Verification passed with `npm run build` and the required web-game Playwright client after the rename. Latest visual check screenshot: `output/web-game/shot-1.png`.
- GitHub CLI was installed with winget, but GitHub repo creation/push is blocked until `gh auth login` is completed for this Windows user.
- Inspector layout fix: moved the selected-item editor into the canvas wrapper instead of positioning it against the full stage, then allowed the top toolbar and tool group to wrap. This prevents the inspector from covering Save/Load/Clear and other top toolbar options. Verification passed with `npm run build`, the required web-game Playwright client, and a viewport layout check confirming the inspector is below the toolbar and Save/Load/Clear remain in view. Screenshot: `output/web-game/inspector-layout-verified.png`.
- Added starter decorated room templates to the Rooms catalog: Kitchen Template and Bathroom Template. Each template places a normal room module plus separate editable child items assigned to that room. The kitchen template includes kitchen-appropriate counters, sink, stove, refrigerator, upper cabinet, island, window, patio door, and plant; the bathroom template includes bathtub, shower, toilet, vanity, rug, window, door, and plant. Verification passed with `npm run build`, required web-game Playwright client, and a targeted template placement test confirming both templates create room-specific editable items with no kitchen fixtures inside the bathroom template. Screenshot: `output/web-game/room-templates-verified.png`.
- Roof clearance fix: replaced the gable roof's rotated box slabs with true sloped roof panel geometry, raised the eave/flat/hip roof baseline above wall height, and added ridge/fascia/gable caps so wall tops no longer protrude through the roof. Verification passed with `npm run build`, required web-game Playwright client, and a targeted shell-view roof placement screenshot. Screenshot: `output/web-game/roof-wall-clearance-verified.png`.
- Roof seating and outdoor build-tools pass: lowered the gable eave so the roof sits on the wall trim instead of floating, made gable end caps double-sided so both sides read as closed, and lowered flat/hip roof baselines. Added SimCity-style drag-build behavior for Fence, Stone Patio, Stone Walkway, and Deck: dragging a fence creates a segmented run, while dragging patio/deck/walkway creates a resized custom surface. Added a Dozer toolbar mode plus `B` hotkey to click/drag-delete unlocked objects, enabling quick fence openings and rebuilds. Verification passed with `npm run build`, required web-game Playwright client, roof seating screenshot, and targeted drag-build/dozer test. Screenshots: `output/web-game/roof-seated-open-side-verified.png` and `output/web-game/drag-build-dozer-verified.png`.
- SimCity-style placement QoL pass: selecting an asset now shows a transparent in-world ghost plus a floor/foundation footprint highlight while hovering over the canvas. Drag-building fences, trees, shrubs, patios, walkways, and decks shows the temporary preview during drag and only commits on mouse release. Trees and shrubs now support chained placement, fence segments use tighter spacing/longer rails to remove visible gaps, and grass/grass plots use a procedural grass texture. The starter room can be unlocked and rotated with the existing rotation controls/hotkeys. Verification passed with `npm run build`, the required web-game Playwright client, and targeted Playwright checks for hover preview, temporary drag preview, fence/tree/shrub runs, and initial-room unlock/rotate. Screenshots: `output/web-game/qol-placement-preview-verified.png`, `output/web-game/qol-drag-runs-verified.png`, and `output/web-game/qol-initial-room-rotate-verified.png`.
- Interior layout and US-measurement pass: added a drag-built Interior Wall asset in Build that snaps one end to a room wall and belongs to that room, so partitions move with room modules like furniture does. Drag-built runs now get a shared group id, so selecting a fence/hedge run selects the whole run for combined movement/deletion. Room width/depth controls now display standard US feet/inches while preserving existing internal units and save compatibility. Shell mode no longer repositions the camera. Variant controls now hide entirely for objects without variants. Added Hedge as an outdoor asset and shrub variants. Multi-delete now removes all selected unlocked items. Walk mode now allows moving outside through passage doors and blocks movement through interior partition walls. Verification passed with `npm run build`, required web-game Playwright client, and targeted Playwright checks for interior wall creation, grouped fence deletion, hedge runs, hidden variant fields, US labels, and shell camera stability. Screenshots: `output/web-game/interior-wall-verified.png` and `output/web-game/walls-groups-hedges-verified.png`.
- Multi-story foundation pass: added Level 1 / Level 2 / All controls to the toolbar. Placement now projects onto the active level plane, rooms and objects carry a saved `level`, and Level 2 room modules appear elevated at the second-story height while preserving the existing Level 1 room. Visibility can show all levels or only the active level. Stairs and Wide Staircase now include a `To Level 2` variant that reaches the second story, and Stair Landing placement adapts to elevated levels. Save/load and `render_game_to_text` now include active level, all-level visibility, and per-object levels. Verification passed with `npm run build`, required web-game Playwright client, and targeted Playwright checks for Level 2 room placement, second-floor stairs, and All-level visibility toggling. Screenshot: `output/web-game/multi-level-verified.png`.

Progress:
- Latest UX/build refinement pass: improved Ctrl/Cmd multi-select so build runs toggle as a group, and selected multi-object groups can rotate together. `Ctrl/Cmd+Z` and `Ctrl/Cmd+Y` now prevent browser defaults and register as app undo/redo, with `Ctrl/Cmd+Shift+Z` also redoing.
- Added a context-aware Length control in the inspector. It appears for interior walls, fence/tree/shrub/hedge build runs, grass plots, decks, patios, walkways, horseshoes, and volleyball nets. Fence/plant runs can be resized after placement, while surfaces update their major dimension without needing delete/rebuild.
- Placement previews can now be rotated before click placement with the same rotation hotkeys, so the ghost orientation matches what will be placed. Room width/depth sliders now go down to 1.5m for closet-sized spaces.
- Stairs now have clearer Full Story Straight and Full Story Switchback variants for reaching Level 2. Wall openings now only affect the owning room unless a passage is genuinely on a shared wall, reducing phantom openings when snapping rooms together. Wall-mounted televisions sit farther inside the room so wall variants do not protrude through the wall.
- Verification passed with `npm run build`, the required web-game Playwright client, screenshot inspection, and targeted Playwright checks for grouped fence selection, post-placement length resizing, `Ctrl+Z` undo, hover-preview rotation, closet-size room resizing, and stair variant selection.
- Current QoL pass: removed the top cap/moulding from drag-built interior walls, while keeping baseboards segmented around openings. Doors/windows/patio doors/archways can now target an interior wall; wall openings store `targetWallId`, rebuild the partition with a cutout, move with the partition, and allow walk-mode passage through partition doors/archways.
- Added a Door variant selector with Open and Closed options. Room modules now appear under Build, while decorated templates remain in Rooms. The bottom helper pill now stays hidden unless a temporary hint is being flashed.
- Undo/redo now preserves the current camera/view placement, so `Ctrl/Cmd+Z` changes the canvas state without snapping the camera to an older view. `Ctrl/Cmd+X` deletes the selected unlocked object(s). Copy/paste now handles multi-selected groups as independent pasted groups, and pasted single chain segments no longer remain part of the original chain.
- Multi-story refinement: reduced the second-level vertical spacing so stacked rooms sit closer to the lower walls, added a first pass at floor openings over full-story stair landings, and added walk-mode stair height interpolation so full-story stairs can carry the first-person camera up toward Level 2.
- Verification passed with `npm run build`, the required web-game Playwright client, screenshot inspection, and targeted Playwright checks for Build-category room modules, interior-wall door targeting, closed-door variant, `Ctrl+X`, camera-preserving `Ctrl+Z`, hidden idle helper text, and Level 2 room placement. Screenshot: `output/web-game/stairs-level2-qol.png`.

TODO:
- Next worthwhile enhancements: stair-aware walk-mode transitions between levels, second-floor openings/railings, multi-level roof generation, more realistic multi-slope roofs for L/T-shaped clusters, visible doorway affordance/highlight while placing doors, smarter collision using rotated bounding boxes, true boolean/CSG wall geometry for precise trim/window cutouts, import/export JSON, material swatches/favorites/recent assets, object search across all categories, randomize variant button, and a GLB/glTF asset pipeline for more realistic furniture.
- Grid-aligned partition pass: interior wall building and movement now snap to the actual room wall core instead of keeping a hidden trim margin, so partitions can touch exterior/interior room walls cleanly. Interior walls inherit the owning room wall color/material and the Length control can span the full room width/depth.
- Placement previews for wall openings now use the same wall-aware hit targeting as final placement, so door/window/patio-door ghosts line up against room and interior walls before click placement. Wall openings and interior walls also accept points on the wall core instead of requiring a point set back inside the room.
- Verification passed with `npm run build`, the required web-game Playwright client, screenshot inspection, and a targeted Playwright check that created an interior wall, resized it to the full 12m room width, and confirmed no console/page errors. Screenshot: `output/web-game/interior-wall-flush-verified.png`.
- QoL/build planning pass: added Favorites and Recent virtual catalog tabs with per-asset favorite controls and recent-use persistence. Added inspector renaming for rooms/items, visible room label sprites, and layer toggles for furniture, build/structure, outdoor, and labels.
- Stair/second-floor workflow now has a clearer visual path: full-story stairs show a translucent Level 2 stairwell guide, and upper-level room floor openings get trim plus guard rails around the stair cutout. Door/window/patio/arch placement previews now draw a translucent cut highlight on the target wall before click placement.
- Started the realistic asset pipeline by adding `public/models/.gitkeep` and `docs/asset-pipeline.md`, documenting GLB scale/origin/export rules, source options, and the next code step for a GLTFLoader registry.
- Verification passed with `npm run build`, the required web-game Playwright client, screenshot inspection, and a targeted Playwright flow for favorites/recent assets, room rename/labels, opening preview highlights, full-story stairs plus Level 2 room placement, and layer toggles. Screenshot: `output/web-game/layers-stairs-favorites-verified.png`.
- Usability polish pass: added a `Go to` room navigator in the room controls that selects and frames rooms by name/level, plus a `Frame` toolbar action that focuses the selected object/room or the whole property.
- Added portable project JSON export/import controls. Export downloads a `.homeforge.json` file using the current project name; Import reads a compatible JSON project, restores the design, and leaves it ready to save as a local named project.
- Added a `Randomize style` inspector action for objects with variants and disabled-state styling so unavailable inspector controls visibly recede.
- Verification passed with `npm run build`, the required web-game Playwright client, screenshot inspection, and a targeted Playwright flow for room navigation, variant randomizing, export download, import restore, and no page/console errors. Screenshot: `output/web-game/polish-qol-verified.png`.
